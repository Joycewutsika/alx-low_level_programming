#include "main.h"
/**
 * mul - checks for multiplication of two integers
 * @a: a is the number
 * @b: b is the number
 * Return: Always 0.
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
